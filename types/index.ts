export type StepperFormData = {
  productPicture?: { fileData: string; fileName: string; mimeType: string };
};

export type ApiResponse = { background: string; product: string; x: number; y: number };
